// Canonical production origin. Kept in one place so canonical/sitemap/OG URLs
// stay in sync — change this single value if the domain ever moves.
export const SITE_URL = 'https://letallgirls.org'
export const SITE_NAME = 'LetAllGirls'

// Absolute path (served from /public) to the default social-share image.
export const DEFAULT_OG_IMAGE = '/og-image.jpg'
